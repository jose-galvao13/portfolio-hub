---
import CaseStudyLayout from '../../layouts/en/CaseStudyLayout.astro';

const metrics = [
  { icon: '⚡', value: '<5 sec', label: 'Processing Time' },
  { icon: '✅', value: 'Automated', label: 'Debit=Credit Validation' },
  { icon: '📊', value: '12', label: 'Financial Statements' },
  { icon: '🎯', value: 'Consistent', label: 'Output Every Run' }
];
---

<CaseStudyLayout
  title="Automated Financial Reporting"
  description="Python automation eliminating 2 days of manual Excel work during month-end close with SNC compliance validation."
  projectType="Accounting Automation"
  stack={['Python', 'Pandas', 'OpenPyXL', 'Financial Reporting']}
  metrics={metrics}
  githubUrl="https://github.com/jose-galvao13/automated-financial-reporting"
  currentLang="en"
>
  <section>
    <h2>🎯 Why I Built This</h2>
    <p>
      I saw this pattern during my accounting internship: month-end close involves manually copying trial balance data across multiple Excel templates and checking it against accounting standards (SNC) by hand, which is slow and leaves room for copy-paste errors. I built this automation to see how much of that could be scripted safely.
    </p>
    <ul>
      <li><strong>Manual Process:</strong> copying figures across templates by hand is repetitive and error-prone.</li>
      <li><strong>No Automated Validation:</strong> checking that debits equal credits, or that accounts reconcile, is usually done by eye.</li>
      <li><strong>Time Cost:</strong> a process like this can easily take a full day or two each month.</li>
    </ul>
  </section>

  <section>
    <h2>💡 Solution Architecture</h2>
    <p>
      Built a <strong>Python automation script</strong> that reads Trial Balance, validates against SNC rules, and generates formatted financial statements in under 5 seconds.
    </p>
    
    <h3>Key Features</h3>
    <ul>
      <li><strong>Trial Balance Validation:</strong> Checks debits = credits, no orphan accounts, balance consistency.</li>
      <li><strong>SNC Compliance:</strong> Maps accounts to correct financial statement lines per Portuguese GAAP.</li>
      <li><strong>Template Population:</strong> Auto-fills 12 Excel templates (Balance Sheet, Income Statement, Cash Flow).</li>
      <li><strong>Formula Verification:</strong> Validates all Excel formulas (Assets = Liabilities + Equity, etc.).</li>
    </ul>

    <h3>Technical Implementation</h3>
    <pre><code>import pandas as pd
import openpyxl

# 1. Read Trial Balance
tb = pd.read_excel('trial_balance.xlsx')

# 2. Validate
assert tb['debit'].sum() == tb['credit'].sum(), "TB doesn't balance!"
assert tb['account_code'].isnull().sum() == 0, "Missing account codes"

# 3. Map to Financial Statements
income_stmt = tb[tb['account_code'].str.startswith('6')].copy()
balance_sheet = tb[~tb['account_code'].str.startswith('6')].copy()

# 4. Populate Templates
wb = openpyxl.load_workbook('BS_template.xlsx')
ws = wb['Balance Sheet']
ws['B5'] = balance_sheet[balance_sheet['account_code'] == '1100']['balance'].sum()
wb.save('Balance_Sheet_Final.xlsx')</code></pre>
  </section>

  <section>
    <h2>📈 What the Script Does</h2>
    <ul>
      <li><strong>~5 Seconds:</strong> the automated version runs in seconds versus the hours a manual pass through the same templates would take.</li>
      <li><strong>Built-in Validation:</strong> checks that debits equal credits and that account codes aren't missing, before anything gets written to the output templates.</li>
      <li><strong>Consistent Output:</strong> the same script produces the same structure every time, removing the "which version is final" problem that comes with manually-edited spreadsheets.</li>
    </ul>
    <p><em>The "€50k saved" and "validated by auditors" figures on the summary card describe the kind of error this validation step is designed to catch — they aren't a real incident I resolved.</em></p>
  </section>

  <section>
    <h2>🚀 Future Enhancements</h2>
    <ul>
      <li><strong>ERP Integration:</strong> Pull Trial Balance directly from SAP API (no manual export).</li>
      <li><strong>Multi-Entity Consolidation:</strong> Automate group-level consolidations with eliminations.</li>
      <li><strong>PDF Generation:</strong> Output board-ready PDF reports with charts.</li>
    </ul>
  </section>
</CaseStudyLayout>
