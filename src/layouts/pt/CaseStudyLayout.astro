---
import Layout from './Layout.astro';

interface Props {
  title: string;
  description: string;
  projectType: string;
  stack: string[];
  metrics: {
    label: string;
    value: string;
    icon: string;
  }[];
  githubUrl: string;
  liveUrl?: string;
}

const { title, description, projectType, stack, metrics, githubUrl, liveUrl } = Astro.props;
---

<Layout title={`${title} - Estudo de Caso | José Galvão`} description={description}>
  <!-- Barra de Progresso de Leitura -->
  <div class="reading-progress" role="progressbar" aria-label="Progresso de leitura" aria-valuenow="0" aria-valuemin="0" aria-valuemax="100"></div>

  <div class="case-study-root">
    
    <!-- Efeitos de Fundo -->
    <div class="bg-effects" aria-hidden="true">
      <div class="glow-orb orb-1"></div>
      <div class="glow-orb orb-2"></div>
      <div class="grid-overlay"></div>
    </div>
    
    <br /> <br />  <br /> <br />
    <header class="ultra-header">
      <div class="container-wide">
        <div class="header-split">
          <div class="title-area">
            <span class="eyebrow">
              <span class="eyebrow-dot" aria-hidden="true"></span>
              {projectType}
              <span class="eyebrow-line" aria-hidden="true"></span>
            </span>
            <h1 class="elegant-title">
              {title}
              <div class="title-underline" aria-hidden="true"></div>
            </h1>
          </div>
          <p class="hero-desc">{description}</p>
        </div>
      </div>
    </header>

    <div class="main-layout container-wide">
      
      <!-- Área de Conteúdo -->
      <article class="technical-content" id="case-study-content">
        <slot />
      </article>

      <!-- Barra Lateral Fixa -->
      <aside class="tech-sidebar" aria-label="Detalhes do projeto">
        <div class="sidebar-inner">
          
          <div class="sidebar-section">
            <h4 class="section-title">
              <span class="title-emoji" aria-hidden="true">📊</span>
              Indicadores-Chave
            </h4>
            <div class="metrics-stack">
              {metrics.map((m, idx) => (
                <div class="m-item" style={`animation-delay: ${idx * 0.1}s`}>
                  <div class="m-content">
                    <div class="metric-bar" role="progressbar" aria-label={m.label} aria-valuenow={Math.min(parseInt(m.value) || 85, 100)} aria-valuemin="0" aria-valuemax="100">
                      <div class="metric-fill" style={`width: ${Math.min(parseInt(m.value) || 85, 100)}%`}></div>
                    </div>
                    <span class="m-val">{m.value}</span>
                    <span class="m-lab">{m.label}</span>
                  </div>
                </div>
              ))}
            </div>
          </div>

          <div class="sidebar-section">
            <h4 class="section-title">
              <span class="title-emoji" aria-hidden="true">⚙️</span>
              Tecnologias Principais
            </h4>
            <div class="stack-cloud">
              {stack.map((s, idx) => (
                <span class="s-tag" style={`animation-delay: ${idx * 0.05}s`}>
                  <span class="tag-shimmer" aria-hidden="true"></span>
                  {s}
                </span>
              ))}
            </div>
          </div>

          <div class="sidebar-section">
            <h4 class="section-title">
              <span class="title-emoji" aria-hidden="true">🔗</span>
              Recursos Externos
            </h4>
            <div class="link-group">
              <a href={githubUrl} target="_blank" rel="noopener noreferrer" class="side-btn github" aria-label={`Ver código-fonte de ${title} no GitHub (abre em novo separador)`}>
                <svg width="18" height="18" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
                  <path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/>
                </svg>
                <span>Ver Repositório</span>
                <svg class="arrow" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
                  <path d="M5 12h14M12 5l7 7-7 7"/>
                </svg>
              </a>
              {liveUrl && (
                <a href={liveUrl} target="_blank" rel="noopener noreferrer" class="side-btn live" aria-label={`Ver demo ao vivo de ${title} (abre em novo separador)`}>
                  <span class="live-indicator" aria-hidden="true"></span>
                  <span>Demo ao Vivo</span>
                  <svg class="arrow" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
                    <path d="M5 12h14M12 5l7 7-7 7"/>
                  </svg>
                </a>
              )}
            </div>
          </div>

          <div class="sidebar-footer">
            <p>Análise por José Galvão</p>
            <div class="status-indicator" aria-label="Projeto ativo">
              <span class="pulse" aria-hidden="true"></span> Projeto Ativo
            </div>
          </div>
        </div>
      </aside>

    </div>
  </div>

</Layout>

<!--
  is:global — necessário porque .copy-code-btn é injetado em runtime por JS.
  O <style> com scope do Astro só se aplica a elementos presentes em build time.
  O botão é inserido diretamente dentro de <pre>, por isso position:relative no pre ancora-o.
-->
<style is:global>
  /* ══════════════════════════════════════════════════════
     BODY LIGHT MODE OVERRIDE — must be global to reach
     the <body> element which lives outside this component
  ══════════════════════════════════════════════════════ */
[data-theme="light"] body {
  background: linear-gradient(-45deg, var(--body-gradient-1), var(--body-gradient-2), var(--body-gradient-3), var(--body-gradient-1)) !important;
  background-size: 400% 400%;
  animation: none !important;   /* ← ESTA LINHA É A QUE FALTA */
  color: var(--text-primary);
}

  [data-theme="light"] body::before {
    background-image:
      radial-gradient(circle at 25% 25%, rgba(14, 165, 233, 0.05) 0%, transparent 50%),
      radial-gradient(circle at 75% 75%, rgba(13, 148, 136, 0.05) 0%, transparent 50%) !important;
  }

  /* ── Copy code button ── */
  pre {
    position: relative !important;
  }

  pre:hover .copy-code-btn {
    opacity: 1 !important;
    pointer-events: auto !important;
  }

  .copy-code-btn {
    position: absolute !important;
    top: 10px !important;
    right: 10px !important;
    z-index: 10 !important;
    display: flex !important;
    align-items: center !important;
    gap: 5px !important;
    padding: 4px 10px 4px 8px !important;
    backdrop-filter: blur(8px) !important;
    -webkit-backdrop-filter: blur(8px) !important;
    border-radius: 6px !important;
    font-family: 'Inter', -apple-system, sans-serif !important;
    font-size: 11px !important;
    font-weight: 500 !important;
    letter-spacing: 0.3px !important;
    line-height: 1 !important;
    cursor: pointer !important;
    opacity: 0 !important;
    pointer-events: none !important;
    transition: opacity 0.2s ease, color 0.2s ease, border-color 0.2s ease, background 0.2s ease !important;
  }

  [data-theme="dark"] .copy-code-btn,
  :root:not([data-theme="light"]) .copy-code-btn {
    background: rgba(8, 8, 8, 0.9) !important;
    border: 1px solid rgba(255, 255, 255, 0.1) !important;
    color: rgba(255, 255, 255, 0.4) !important;
  }

  [data-theme="light"] .copy-code-btn {
    background: rgba(240, 244, 248, 0.95) !important;
    border: 1px solid rgba(0, 0, 0, 0.12) !important;
    color: rgba(30, 40, 55, 0.5) !important;
  }

  .copy-code-btn:hover {
    color: #ffffff !important;
    border-color: rgba(0, 242, 255, 0.5) !important;
    background: rgba(0, 242, 255, 0.08) !important;
  }

  [data-theme="light"] .copy-code-btn:hover {
    color: #1a1f2e !important;
    border-color: rgba(14, 165, 233, 0.5) !important;
    background: rgba(14, 165, 233, 0.08) !important;
  }

  .copy-code-btn.copied {
    color: #00ff88 !important;
    border-color: rgba(0, 255, 136, 0.4) !important;
    background: rgba(0, 255, 136, 0.06) !important;
    opacity: 1 !important;
    pointer-events: none !important;
  }

  .copy-code-btn svg {
    width: 12px !important;
    height: 12px !important;
    flex-shrink: 0 !important;
    pointer-events: none !important;
    display: block !important;
  }
</style>

<style>
  /* ── CSS VARIABLES ── */
  :root,
  [data-theme="dark"] {
    --cs-bg: #030303;
    --cs-text: #ffffff;
    --cs-text-dim: #9da5b0;
    --cs-accent: #00f2ff;
    --cs-accent-glow: rgba(0, 242, 255, 0.3);
    --cs-accent-bright: #00ffff;
    --cs-border: rgba(255, 255, 255, 0.07);
    --cs-highlight-bg: rgba(0, 242, 255, 0.05);
    --cs-code-inline-bg: rgba(255, 255, 255, 0.05);
    --cs-code-block-bg: #080808;
    --cs-blockquote-bg: rgba(255, 255, 255, 0.02);
    --cs-table-bg: rgba(255, 255, 255, 0.02);
    --cs-table-head-bg: rgba(0, 242, 255, 0.1);
    --cs-table-row-hover: rgba(0, 242, 255, 0.05);
    --cs-sidebar-bg: rgba(255, 255, 255, 0.02);
    --cs-sidebar-border: rgba(255, 255, 255, 0.07);
    --cs-sidebar-hover-border: rgba(0, 242, 255, 0.3);
    --cs-metric-bg: rgba(255, 255, 255, 0.05);
    --cs-tag-bg: rgba(255, 255, 255, 0.05);
    --cs-tag-border: rgba(255, 255, 255, 0.07);
    --cs-tag-color: #ffffff;
    --cs-link-github-bg: #fff;
    --cs-link-github-color: #000;
    --cs-link-live-bg: rgba(0, 242, 255, 0.1);
    --cs-link-live-color: #00f2ff;
    --cs-link-live-border: #00f2ff;
    --cs-footer-border: rgba(255, 255, 255, 0.07);
    --cs-footer-text: #9da5b0;
    --cs-pre-border: rgba(255, 255, 255, 0.07);
    --cs-progress-shadow: rgba(0, 242, 255, 0.3);
    --cs-orb1-color: rgba(0, 242, 255, 0.3);
    --cs-orb2-color: rgba(136, 0, 255, 0.2);
    --cs-grid-color: rgba(255, 255, 255, 0.07);
    --cs-header-before: rgba(0, 242, 255, 0.08);
  }

  [data-theme="light"] {
    --cs-bg: #f0f4f9;
    --cs-text: #1a1f2e;
    --cs-text-dim: #5a6478;
    --cs-accent: #0284c7;
    --cs-accent-glow: rgba(14, 165, 233, 0.25);
    --cs-accent-bright: #0ea5e9;
    --cs-border: rgba(0, 0, 0, 0.07);
    --cs-highlight-bg: rgba(14, 165, 233, 0.05);
    --cs-code-inline-bg: rgba(14, 165, 233, 0.08);
    --cs-code-block-bg: #e2e8f0;
    --cs-blockquote-bg: rgba(255, 255, 255, 0.8);
    --cs-table-bg: rgba(255, 255, 255, 0.8);
    --cs-table-head-bg: rgba(14, 165, 233, 0.08);
    --cs-table-row-hover: rgba(14, 165, 233, 0.04);
    --cs-sidebar-bg: rgba(255, 255, 255, 0.9);
    --cs-sidebar-border: rgba(0, 0, 0, 0.08);
    --cs-sidebar-hover-border: rgba(14, 165, 233, 0.4);
    --cs-metric-bg: rgba(0, 0, 0, 0.04);
    --cs-tag-bg: rgba(14, 165, 233, 0.06);
    --cs-tag-border: rgba(14, 165, 233, 0.15);
    --cs-tag-color: #1a1f2e;
    --cs-link-github-bg: #1a1f2e;
    --cs-link-github-color: #fff;
    --cs-link-live-bg: rgba(14, 165, 233, 0.08);
    --cs-link-live-color: #0284c7;
    --cs-link-live-border: #0284c7;
    --cs-footer-border: rgba(0, 0, 0, 0.07);
    --cs-footer-text: #5a6478;
    --cs-pre-border: rgba(0, 0, 0, 0.08);
    --cs-progress-shadow: rgba(14, 165, 233, 0.25);
    --cs-orb1-color: rgba(14, 165, 233, 0.12);
    --cs-orb2-color: rgba(100, 0, 200, 0.06);
    --cs-grid-color: rgba(0, 0, 0, 0.03);
    --cs-header-before: rgba(14, 165, 233, 0.05);
  }

  * { box-sizing: border-box; }

  .case-study-root {
    background: var(--cs-bg);
    color: var(--cs-text);
    font-family: 'Inter', -apple-system, sans-serif;
    min-height: 100vh;
    position: relative;
    overflow-x: hidden;
    width: 100%;
    transition: background 0.3s ease, color 0.3s ease;
    /* Ensure it sits above the body background */
    isolation: isolate;
  }

  .container-wide {
    max-width: 1300px;
    margin: 0 auto;
    padding: 0 40px;
    position: relative;
    z-index: 2;
    width: 100%;
  }

  /* ── BACKGROUND EFFECTS ── */
  /* In light mode, bg-effects is behind case-study-root content
     but we still need it to not bleed through */
  .bg-effects {
    position: fixed;
    top: 0; left: 0;
    width: 100%; height: 100%;
    z-index: 0;
    pointer-events: none;
    overflow: hidden;
  }

  /* Light mode: hide the dark bg-effects entirely */
  [data-theme="light"] .bg-effects {
    display: none;
  }

  .glow-orb {
    position: absolute;
    border-radius: 50%;
    filter: blur(100px);
    opacity: 0.4;
    animation: float 20s ease-in-out infinite;
  }

  .orb-1 {
    top: 10%; left: 15%;
    width: 500px; height: 500px;
    background: radial-gradient(circle, var(--cs-orb1-color), transparent);
  }

  .orb-2 {
    bottom: 20%; right: 10%;
    width: 400px; height: 400px;
    background: radial-gradient(circle, var(--cs-orb2-color), transparent);
    animation-delay: -10s;
  }

  @keyframes float {
    0%, 100% { transform: translate(0, 0) scale(1); }
    33% { transform: translate(40px, -60px) scale(1.1); }
    66% { transform: translate(-30px, 40px) scale(0.9); }
  }

  .grid-overlay {
    position: absolute;
    inset: 0;
    background-image: 
      linear-gradient(var(--cs-grid-color) 1px, transparent 1px),
      linear-gradient(90deg, var(--cs-grid-color) 1px, transparent 1px);
    background-size: 50px 50px;
    mask-image: radial-gradient(ellipse at center, black 20%, transparent 70%);
    opacity: 0.3;
  }

  .reading-progress {
    position: fixed;
    top: 0; left: 0; width: 0%; height: 3px;
    background: linear-gradient(90deg, var(--cs-accent), var(--cs-accent-bright), #00ff88);
    z-index: 1000;
    box-shadow: 0 0 20px var(--cs-progress-shadow);
    transition: width 0.1s ease;
  }

  /* ── HEADER ── */
  .ultra-header {
    padding: 120px 0 80px;
    border-bottom: 1px solid var(--cs-border);
    position: relative;
    width: 100%;
  }

  .ultra-header::before {
    content: '';
    position: absolute;
    top: 0; left: 0;
    width: 100%; height: 100%;
    background: radial-gradient(circle at 0% 0%, var(--cs-header-before) 0%, transparent 50%);
    pointer-events: none;
  }

  .header-split {
    display: grid; 
    grid-template-columns: 1.5fr 1fr; 
    gap: 60px; 
    align-items: end;
    width: 100%;
  }

  .eyebrow {
    display: flex;
    align-items: center;
    gap: 12px;
    color: var(--cs-accent);
    text-transform: uppercase;
    font-size: 12px;
    font-weight: 800;
    letter-spacing: 4px;
    margin-bottom: 24px;
  }

  .eyebrow-dot {
    width: 8px; height: 8px;
    background: var(--cs-accent);
    border-radius: 50%;
    animation: pulse-dot 2s ease-in-out infinite;
    box-shadow: 0 0 10px var(--cs-accent-glow);
  }

  @keyframes pulse-dot {
    0%, 100% { box-shadow: 0 0 0 0 var(--cs-accent-glow); transform: scale(1); }
    50% { box-shadow: 0 0 0 8px transparent; transform: scale(1.2); }
  }

  .eyebrow-line {
    flex: 1;
    height: 2px;
    background: linear-gradient(90deg, var(--cs-accent), transparent);
    max-width: 80px;
  }

  .elegant-title {
    font-size: clamp(2.5rem, 6vw, 5rem);
    line-height: 0.95;
    font-weight: 900;
    letter-spacing: -3px;
    margin: 0;
    position: relative;
    display: inline-block;
    animation: elegantReveal 0.9s cubic-bezier(0.16, 1, 0.3, 1) both;
    max-width: 100%;
    word-wrap: break-word;
  }

  /* Dark mode title gradient */
  [data-theme="dark"] .elegant-title,
  :root:not([data-theme="light"]) .elegant-title {
    background: linear-gradient(135deg, #ffffff 0%, #a8d8ea 40%, #00bfa5 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  /* Light mode title — dark enough to read on light bg */
  [data-theme="light"] .elegant-title {
    background: linear-gradient(135deg, #0f172a 0%, #0284c7 55%, #0d9488 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  @keyframes elegantReveal {
    from { opacity: 0; transform: translateY(24px); filter: blur(8px); }
    to { opacity: 1; transform: translateY(0); filter: blur(0); }
  }

  .title-underline {
    position: absolute;
    bottom: -12px; left: 0;
    width: 120px; height: 4px;
    background: linear-gradient(90deg, var(--cs-accent), transparent);
    border-radius: 2px;
    animation: underlineSlide 2s ease-in-out infinite;
  }

  @keyframes underlineSlide {
    0%, 100% { width: 120px; }
    50% { width: 200px; }
  }

  .hero-desc {
    font-size: clamp(1rem, 2vw, 1.25rem);
    color: var(--cs-text-dim);
    line-height: 1.6;
    margin: 0;
    position: relative;
    padding-left: 24px;
    animation: elegantReveal 0.9s 0.2s cubic-bezier(0.16, 1, 0.3, 1) both;
  }

  .hero-desc::before {
    content: '';
    position: absolute;
    left: 0; top: 0;
    width: 3px; height: 100%;
    background: linear-gradient(180deg, var(--cs-accent), transparent);
  }

  /* ── MAIN LAYOUT ── */
  .main-layout {
    display: grid;
    grid-template-columns: 1fr 320px;
    gap: 80px;
    padding-top: 80px;
    padding-bottom: 80px;
    width: 100%;
    position: relative;
  }

  .technical-content {
    min-width: 0;
    width: 100%;
  }

  .technical-content :global(h2) {
    font-size: clamp(1.5rem, 3vw, 2rem);
    font-weight: 800;
    margin: 60px 0 30px;
    color: var(--cs-text);
    position: relative;
    padding-bottom: 16px;
    display: flex;
    align-items: center;
    gap: 12px;
    flex-wrap: wrap;
  }

  .technical-content :global(h2:first-of-type) { margin-top: -10px; padding-top: 0; }

  .technical-content :global(h2::after) {
    content: '';
    position: absolute;
    bottom: 0; left: 0;
    width: 60px; height: 3px;
    background: var(--cs-accent);
    border-radius: 2px;
  }

  .technical-content :global(h3) {
    font-size: clamp(1.25rem, 2.5vw, 1.5rem);
    margin: 40px 0 20px;
    font-weight: 700;
    color: var(--cs-text);
  }

  .technical-content :global(h4) {
    font-size: clamp(1.1rem, 2vw, 1.3rem);
    margin: 30px 0 16px;
    font-weight: 600;
    color: var(--cs-accent);
  }

  .technical-content :global(p) {
    font-size: clamp(1rem, 1.5vw, 1.15rem);
    line-height: 1.8;
    color: var(--cs-text-dim);
    margin-bottom: 30px;
    max-width: 100%;
    word-wrap: break-word;
  }

  .technical-content :global(strong),
  .technical-content :global(b) {
    color: var(--cs-text);
    font-weight: 700;
  }

  /* Gradient on strong only in dark mode */
  [data-theme="dark"] .technical-content :global(strong),
  [data-theme="dark"] .technical-content :global(b),
  :root:not([data-theme="light"]) .technical-content :global(strong),
  :root:not([data-theme="light"]) .technical-content :global(b) {
    background: linear-gradient(135deg, var(--cs-accent-bright), var(--cs-accent));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  /* Light mode strong: solid readable colour, no gradient */
  [data-theme="light"] .technical-content :global(strong),
  [data-theme="light"] .technical-content :global(b) {
    color: #0f172a;
    -webkit-text-fill-color: #0f172a;
    background: none;
  }

  .technical-content :global(p code),
  .technical-content :global(li code),
  .technical-content :global(td code) {
    background: var(--cs-code-inline-bg);
    border: 1px solid var(--cs-border);
    padding: 2px 8px;
    border-radius: 4px;
    font-family: 'Fira Code', 'Courier New', monospace;
    font-size: 0.9em;
    color: var(--cs-accent-bright);
    font-weight: 500;
  }

  /* Light mode inline code: readable dark text */
  [data-theme="light"] .technical-content :global(p code),
  [data-theme="light"] .technical-content :global(li code),
  [data-theme="light"] .technical-content :global(td code) {
    color: #0284c7;
  }

  .technical-content :global(ul),
  .technical-content :global(ol) {
    max-width: 100%;
    padding-left: 28px;
    margin-bottom: 30px;
  }

  .technical-content :global(li) {
    font-size: clamp(1rem, 1.5vw, 1.15rem);
    line-height: 1.8;
    margin-bottom: 16px;
    word-wrap: break-word;
    color: var(--cs-text-dim);
    position: relative;
  }

  .technical-content :global(ul > li::marker) { color: var(--cs-accent); font-size: 1.2em; }
  .technical-content :global(ol > li::marker) { color: var(--cs-accent); font-weight: 700; }
  .technical-content :global(li > ul),
  .technical-content :global(li > ol) { margin-top: 12px; margin-bottom: 12px; }

  .technical-content :global(blockquote) {
    margin: 50px 0;
    padding: 40px;
    background: var(--cs-blockquote-bg);
    border-left: 4px solid var(--cs-accent);
    font-style: italic;
    font-size: clamp(1.1rem, 2vw, 1.3rem);
    border-radius: 0 16px 16px 0;
    position: relative;
    box-shadow: 0 0 30px var(--cs-accent-glow);
    word-wrap: break-word;
  }

  .technical-content :global(blockquote p) { margin-bottom: 0; color: var(--cs-text); }

  .technical-content :global(pre) {
    background: var(--cs-code-block-bg) !important;
    padding: 44px 30px 30px !important;
    border-radius: 12px;
    border: 1px solid var(--cs-pre-border);
    margin: 40px 0;
    font-size: clamp(12px, 1.5vw, 14px);
    box-shadow: 0 20px 50px rgba(0, 0, 0, 0.2);
    overflow-x: auto;
    max-width: 100%;
  }

  [data-theme="light"] .technical-content :global(pre) {
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
  }

  .technical-content :global(pre::before) {
    content: '';
    position: absolute;
    top: 0; left: 0; right: 0;
    height: 1px;
    background: linear-gradient(90deg, transparent, var(--cs-accent), transparent);
  }

  .technical-content :global(pre code) {
    background: transparent;
    border: none;
    padding: 0;
    color: var(--cs-text);
    word-wrap: break-word;
    overflow-wrap: break-word;
    font-family: 'Fira Code', 'Courier New', monospace;
  }

  .technical-content :global(table) {
    width: 100%;
    border-collapse: collapse;
    margin: 40px 0;
    background: var(--cs-table-bg);
    border-radius: 12px;
    overflow: hidden;
    border: 1px solid var(--cs-border);
  }

  .technical-content :global(thead) { background: var(--cs-table-head-bg); }

  .technical-content :global(th) {
    padding: 16px;
    text-align: left;
    font-weight: 700;
    color: var(--cs-accent);
    border-bottom: 2px solid var(--cs-accent);
    font-size: clamp(0.9rem, 1.5vw, 1rem);
  }

  .technical-content :global(td) {
    padding: 14px 16px;
    border-bottom: 1px solid var(--cs-border);
    color: var(--cs-text-dim);
    font-size: clamp(0.9rem, 1.5vw, 1rem);
  }

  .technical-content :global(tr:last-child td) { border-bottom: none; }
  .technical-content :global(tbody tr:hover) { background: var(--cs-table-row-hover); }

  .technical-content :global(.highlight-box),
  .technical-content :global(.info-box),
  .technical-content :global(.warning-box) {
    padding: 24px 28px;
    margin: 30px 0;
    border-radius: 12px;
    border-left: 4px solid var(--cs-accent);
    background: var(--cs-highlight-bg);
    position: relative;
  }

  .technical-content :global(.warning-box) {
    border-left-color: #fbbf24;
    background: rgba(251, 191, 36, 0.05);
  }

  .technical-content :global(.highlight-box p),
  .technical-content :global(.info-box p),
  .technical-content :global(.warning-box p) { margin-bottom: 0; }

  .technical-content :global(img) {
    max-width: 100%;
    height: auto;
    border-radius: 12px;
    margin: 40px 0;
    border: 1px solid var(--cs-border);
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.15);
  }

  .technical-content :global(hr) {
    border: none;
    height: 1px;
    background: linear-gradient(90deg, transparent, var(--cs-border), transparent);
    margin: 60px 0;
  }

  /* ── SIDEBAR ── */
  .tech-sidebar { min-width: 0; height: fit-content; }

  .sidebar-inner {
    position: sticky;
    top: 40px;
    background: var(--cs-sidebar-bg);
    border: 1px solid var(--cs-sidebar-border);
    padding: 30px;
    border-radius: 24px;
    backdrop-filter: blur(20px);
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.1), inset 0 1px 0 rgba(255, 255, 255, 0.05);
    transition: all 0.3s ease;
    width: 100%;
  }

  [data-theme="light"] .sidebar-inner {
    box-shadow: 0 4px 24px rgba(0, 0, 0, 0.07), inset 0 1px 0 rgba(255, 255, 255, 0.8);
  }

  .sidebar-inner:hover {
    border-color: var(--cs-sidebar-hover-border);
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.1), 0 0 40px var(--cs-accent-glow);
  }

  .section-title {
    font-size: 11px;
    text-transform: uppercase;
    letter-spacing: 2px;
    color: var(--cs-accent);
    margin-bottom: 24px;
    font-weight: 700;
    display: flex;
    align-items: center;
    gap: 8px;
  }

  .title-emoji { font-size: 16px; line-height: 1; }

  .sidebar-section {
    margin-bottom: 0;
    padding-bottom: 32px;
    border-bottom: 1px solid var(--cs-border);
  }

  .sidebar-section:last-of-type { border-bottom: none; padding-bottom: 0; }
  .sidebar-section + .sidebar-section { padding-top: 32px; }

  .metrics-stack { display: grid; gap: 24px; }

  .m-item {
    border-bottom: 1px solid var(--cs-border);
    padding-bottom: 18px;
    position: relative;
    animation: fadeInUp 0.6s ease-out backwards;
  }

  .m-content { display: flex; flex-direction: column; min-width: 0; }

  @keyframes fadeInUp {
    from { opacity: 0; transform: translateY(20px); }
    to { opacity: 1; transform: translateY(0); }
  }

  .metric-bar {
    height: 4px;
    background: var(--cs-metric-bg);
    border-radius: 2px;
    overflow: hidden;
    margin-bottom: 12px;
    width: 100%;
  }

  .metric-fill {
    height: 100%;
    background: linear-gradient(90deg, var(--cs-accent), var(--cs-accent-bright));
    border-radius: 2px;
    animation: fillBar 1.5s ease-out;
    box-shadow: 0 0 10px var(--cs-accent-glow);
  }

  @keyframes fillBar { from { width: 0%; } }

  .m-val {
    display: block;
    font-size: clamp(1.5rem, 3vw, 2rem);
    font-weight: 900;
    color: var(--cs-accent);
    text-shadow: 0 0 20px var(--cs-accent-glow);
    word-wrap: break-word;
  }

  [data-theme="light"] .m-val {
    text-shadow: none;
  }

  .m-lab {
    font-size: 12px;
    color: var(--cs-text-dim);
    text-transform: uppercase;
    font-weight: 600;
    word-wrap: break-word;
  }

  .stack-cloud { display: flex; flex-wrap: wrap; gap: 8px; }

  .s-tag {
    background: var(--cs-tag-bg);
    padding: 8px 14px;
    border-radius: 8px;
    font-size: 12px;
    font-weight: 600;
    color: var(--cs-tag-color);
    border: 1px solid var(--cs-tag-border);
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
    animation: tagFadeIn 0.5s ease-out backwards;
  }

  @keyframes tagFadeIn {
    from { opacity: 0; transform: scale(0.8); }
    to { opacity: 1; transform: scale(1); }
  }

  .tag-shimmer {
    position: absolute;
    top: -50%; left: -50%;
    width: 200%; height: 200%;
    background: linear-gradient(45deg, transparent, rgba(255, 255, 255, 0.1), transparent);
    transform: rotate(45deg);
    animation: shimmer 3s infinite;
  }

  @keyframes shimmer {
    0% { transform: translateX(-100%) rotate(45deg); }
    100% { transform: translateX(100%) rotate(45deg); }
  }

  .s-tag:hover {
    background: rgba(0, 242, 255, 0.1);
    border-color: var(--cs-accent);
    transform: translateY(-2px);
    box-shadow: 0 5px 15px var(--cs-accent-glow);
  }

  [data-theme="light"] .s-tag:hover {
    background: rgba(14, 165, 233, 0.12);
  }

  .link-group { display: flex; flex-direction: column; gap: 12px; }

  .side-btn {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 10px;
    padding: 16px 18px;
    border-radius: 12px;
    text-decoration: none;
    font-weight: 700;
    font-size: 14px;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    position: relative;
    overflow: hidden;
  }

  .side-btn::before {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(135deg, rgba(255, 255, 255, 0.1), transparent);
    opacity: 0;
    transition: opacity 0.3s;
  }

  .side-btn:hover::before { opacity: 1; }
  .side-btn:focus-visible { outline: 2px solid var(--cs-accent); outline-offset: 2px; }

  .github {
    background: var(--cs-link-github-bg);
    color: var(--cs-link-github-color);
    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
  }
  .github:hover { transform: translateY(-3px); box-shadow: 0 8px 25px rgba(0, 0, 0, 0.2); }

  .live {
    background: var(--cs-link-live-bg);
    color: var(--cs-link-live-color);
    border: 1px solid var(--cs-link-live-border);
  }
  .live:hover { transform: translateY(-3px); box-shadow: 0 8px 25px var(--cs-accent-glow); }

  .live-indicator {
    width: 8px; height: 8px;
    background: var(--cs-accent);
    border-radius: 50%;
    animation: pulse-glow 2s infinite;
  }

  @keyframes pulse-glow {
    0%, 100% { opacity: 1; transform: scale(1); }
    50% { opacity: 0.6; transform: scale(1.3); }
  }

  .side-btn .arrow { transition: transform 0.3s; }
  .side-btn:hover .arrow { transform: translateX(4px); }

  .sidebar-footer {
    margin-top: 40px;
    padding-top: 30px;
    border-top: 1px solid var(--cs-footer-border);
    font-size: 12px;
    color: var(--cs-footer-text);
    text-align: center;
  }

  .status-indicator {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    margin-top: 12px;
    color: #00ff88;
    font-weight: 700;
    padding: 6px 14px;
    border-radius: 20px;
    border: 1px solid #00ff88;
    font-size: 0.75rem;
    user-select: none;
    -webkit-user-select: none;
    cursor: default;
  }

  [data-theme="light"] .status-indicator {
    color: #059669;
    border-color: #059669;
  }

  .pulse {
    position: relative;
    width: 8px; height: 8px;
    border-radius: 50%;
    background: #00ff88;
    flex-shrink: 0;
  }

  [data-theme="light"] .pulse {
    background: #059669;
  }

  .pulse::after {
    content: '';
    position: absolute;
    top: 50%; left: 50%;
    width: 8px; height: 8px;
    border-radius: 50%;
    background: #00ff88;
    transform: translate(-50%, -50%) scale(1);
    opacity: 0.8;
    animation: pulse-ring 2s ease-out infinite;
  }

  [data-theme="light"] .pulse::after {
    background: #059669;
  }

  @keyframes pulse-ring {
    0% { transform: translate(-50%, -50%) scale(1); opacity: 0.8; }
    100% { transform: translate(-50%, -50%) scale(3.5); opacity: 0; }
  }

  /* ── RESPONSIVE ── */
  @media (max-width: 1024px) {
    .header-split { grid-template-columns: 1fr; }
    .main-layout { grid-template-columns: 1fr; gap: 40px; }
    .sidebar-inner { position: static; }
  }

  @media (max-width: 768px) {
    .container-wide { padding: 0 20px; }
    .ultra-header { padding: 80px 0 60px; }
    .header-split { gap: 30px; }
    .hero-desc { padding-left: 16px; }
    .main-layout { gap: 30px; padding-top: 40px; }
    .sidebar-inner { padding: 20px; }
    .technical-content :global(blockquote) { padding: 24px; }
    .technical-content :global(pre) { padding: 44px 20px 20px !important; }
    .technical-content :global(ul),
    .technical-content :global(ol) { padding-left: 20px; }
  }

  @media (max-width: 480px) {
    .container-wide { padding: 0 16px; }
    .ultra-header { padding: 60px 0 40px; }
    .eyebrow { font-size: 10px; letter-spacing: 2px; }
    .sidebar-inner { padding: 16px; border-radius: 16px; }
    .section-title { font-size: 10px; margin-bottom: 16px; }
    .title-emoji { font-size: 14px; }
    .metrics-stack { gap: 16px; }
    .m-item { padding-bottom: 12px; }
    .metric-bar { height: 3px; margin-bottom: 8px; }
    .stack-cloud { gap: 6px; }
    .s-tag { font-size: 10px; padding: 6px 10px; border-radius: 6px; }
    .side-btn { padding: 12px 14px; font-size: 12px; }
    .sidebar-footer { margin-top: 24px; padding-top: 20px; font-size: 11px; }
    .status-indicator { padding: 6px 12px; font-size: 11px; }
    .technical-content :global(blockquote) { padding: 20px; margin: 30px 0; }
    .technical-content :global(pre) { padding: 44px 16px 16px !important; margin: 30px 0; }
    .technical-content :global(ul),
    .technical-content :global(ol) { padding-left: 16px; }
  }

  @media (prefers-reduced-motion: reduce) {
    .elegant-title, .hero-desc { animation: none; opacity: 1; transform: none; filter: none; }
    .glow-orb { animation: none; }
    .eyebrow-dot { animation: none; }
    .title-underline { animation: none; width: 120px; }
    .metric-fill { animation: none; }
  }
</style>

<script>
  function initCodeCopy() {
    const codeBlocks = document.querySelectorAll('pre');

    codeBlocks.forEach((block) => {
      if (block.querySelector('.copy-code-btn')) return;

      const copyIcon = `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="9" y="9" width="13" height="13" rx="2"/><path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"/></svg>`;
      const checkIcon = `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg>`;

      const button = document.createElement('button');
      button.className = 'copy-code-btn';
      button.type = 'button';
      button.setAttribute('aria-label', 'Copiar código para a área de transferência');
      button.innerHTML = `${copyIcon}<span>Copiar</span>`;

      block.insertBefore(button, block.firstChild);

      button.addEventListener('click', async (e) => {
        e.stopPropagation();

        const codeEl = block.querySelector('code');
        const codeText = codeEl
          ? codeEl.innerText
          : Array.from(block.childNodes)
              .filter((node) => node !== button)
              .map((node) => ('innerText' in node ? (node as HTMLElement).innerText : node.textContent ?? ''))
              .join('');

        try {
          await navigator.clipboard.writeText(codeText.trim());
          button.classList.add('copied');
          button.innerHTML = `${checkIcon}<span>Copiado!</span>`;
          setTimeout(() => {
            button.classList.remove('copied');
            button.innerHTML = `${copyIcon}<span>Copiar</span>`;
          }, 2000);
        } catch (err) {
          console.error('Falha ao copiar:', err);
        }
      });
    });
  }

  function initReadingProgress() {
    const bar = document.querySelector('.reading-progress') as HTMLElement;
    if (!bar) return;
    const updateProgress = () => {
      const winScroll = document.documentElement.scrollTop || document.body.scrollTop;
      const height = document.documentElement.scrollHeight - document.documentElement.clientHeight;
      const scrolled = Math.round((winScroll / height) * 100);
      bar.style.width = scrolled + '%';
      bar.setAttribute('aria-valuenow', String(scrolled));
    };
    window.addEventListener('scroll', updateProgress, { passive: true });
  }

  function initSidebarSticky() {
    const sidebar = document.querySelector('.sidebar-inner') as HTMLElement;
    const container = document.querySelector('.main-layout') as HTMLElement;
    if (!sidebar || !container) return;

    const handleSidebar = () => {
      if (window.innerWidth <= 1024) { sidebar.classList.remove('is-released'); return; }
      const winScroll = window.scrollY || document.documentElement.scrollTop;
      const height = document.documentElement.scrollHeight - document.documentElement.clientHeight;
      const scrollPercent = (winScroll / height) * 100;
      sidebar.classList.toggle('is-released', scrollPercent >= 80);
    };

    window.addEventListener('scroll', () => window.requestAnimationFrame(handleSidebar), { passive: true });
    window.addEventListener('resize', handleSidebar);
    handleSidebar();
  }

  function initNavigationFixes() {
    const navLinks = document.querySelectorAll('.nav-links a[href^="#"], .nav-cta-glow[href^="#"]');
    const logoLink = document.querySelector('.nav-logo');
    const currentPath = window.location.pathname;
    const basePath = currentPath.includes('/portfolio-hub') ? '/portfolio-hub/' : '/';
    const isHomepage =
      currentPath === basePath ||
      currentPath === basePath + 'index.html' ||
      currentPath.endsWith('/portfolio-hub') ||
      currentPath === '/' ||
      currentPath === '/index.html';

    navLinks.forEach(link => {
      link.addEventListener('click', (e) => {
        const hash = link.getAttribute('href');
        if (isHomepage) {
          e.preventDefault();
          document.querySelector(hash || '')?.scrollIntoView({ behavior: 'smooth', block: 'start' });
        } else {
          window.location.href = basePath + hash;
        }
      });
    });

    if (logoLink && !isHomepage) {
      logoLink.addEventListener('click', (e) => {
        e.preventDefault();
        window.location.href = basePath;
      });
    }
  }

  document.addEventListener('DOMContentLoaded', () => {
    initCodeCopy();
    initReadingProgress();
    initSidebarSticky();
    initNavigationFixes();
  });
</script>